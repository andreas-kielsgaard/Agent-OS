# Skill: bounded-review-workflow

## Purpose

Use before final review when exhaustive checking would be noisy.

## Active Command Support

Use adapter-declared verification-plan evidence for per-change checks and repository-health evidence when whole-repository health matters.

## How

Use the active evidence sources named by the skill map. Inspect source/config directly for semantic judgment. Treat tool output as bounded evidence, not authority.
